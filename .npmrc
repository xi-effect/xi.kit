# xi.actions/front-lint-and-format runs `npm install --no-optional` and deletes
# the lockfile. That skips @rollup/rollup-linux-x64-gnu, so workspace `prepare`
# builds fail on Linux CI. Skipping lifecycle scripts avoids that path; build
# explicitly where needed (e.g. publish workflow).
ignore-scripts=true
